{
  "name": "ians-custom-room-card",
  "version": "0.2.1",
  "description": "A highly customizable Home Assistant Lovelace room card with icon, badge, sub-button groups, animations, templates, and more",
  "keywords": ["home-assistant", "lovelace", "custom-card", "hacs"],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "watch": "vite build --watch",
    "dev": "vite"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "terser": "^5.30.0"
  }
}
